-- lists all rows of first_table from the database in the  SQL server
-- All fields should be printed
-- The database name will be passed as an argument of the mysql command

SELECT * FROM first_table;
